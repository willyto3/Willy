import { AreamouseDirective } from './areamouse.directive';

describe('AreamouseDirective', () => {
  it('should create an instance', () => {
    const directive = new AreamouseDirective();
    expect(directive).toBeTruthy();
  });
});
